# 山海行 · 文旅多 Agent 行程规划系统 — 项目报告

> 报告版本：v1.0 ｜ 报告日期：2026-09-08 ｜ 项目版本：v4.0（2026-09-05）
> 资料依据：项目讲解 v4.0、需求与技术方案说明书 v1.0、产品介绍 v3.1、交付文档 v2.0、README

---

## 目录

1. [项目概述](#一项目概述)
2. [需求分析](#二需求分析)
3. [技术架构](#三技术架构)
4. [核心功能](#四核心功能)
5. [数据模型](#五数据模型)
6. [API 与前端](#六api-与前端)
7. [测试与验证结果](#七测试与验证结果)
8. [项目亮点与设计决策](#八项目亮点与设计决策)

---

## 一、项目概述

### 1.1 项目定位

**山海行**是一款基于 **8 个 AI Agent 协作**的文旅资源调研与个性化行程规划系统。用户只需用一句话、一个表单、一次对话甚至一段语音说出旅行需求，系统即可自动完成：

> 需求解析 → 真实数据调研 → 个性化行程编排 → 预算计算 → 风险审核 → 生成完整报告

系统全程使用**真实数据**驱动（高德地图景点/路线/天气 + 阿里云百炼大模型），不是假数据拼凑；支持断点续传、人工审核（HITL）、跨行程图记忆、运行态强干预、人群个性化适配与 RAG 检索增强。

三个关键标签：

| 标签 | 含义 |
|---|---|
| 多 Agent | 不是一个大模型干所有事，而是 8 个分工明确的 Agent 按 DAG 有向无环图协作 |
| 真实数据 | 景点/路线/天气/营业时间/评分/图片全部来自高德地图 API |
| 个性化 | 感知同行老人/儿童/学生，按年龄/身高分档门票、过滤景点、推荐兴趣 |

### 1.2 项目背景与痛点

传统文旅行程规划是纯人工劳动：顾问接需求 → 搜景点/酒店/交通/天气 → 看口碑避坑 → 逐天排行程算预算 → 检查风险 → 出方案。一个行程从需求到方案需几小时到一整天，且存在三大致命缺陷：**数据会过时、没有风险拦截、查到一半崩溃全丢**。

项目围绕 **7 大痛点**展开：

| # | 痛点 | 具体表现 | 本项目解法 |
|---|---|---|---|
| 1 | 语义遗忘 | 用户说"我母亲海鲜严重过敏"，两周后再规划，系统忘了 | 图记忆：跨行程沉淀知识图谱，下次自动召回约束 |
| 2 | 状态不刷新 | 行程跑到一半某景点"台风停运"，系统不知道 | 运行态强干预：主管带验签实时修改运行中行程 |
| 3 | 长任务无容灾 | 8 个 Agent 跑几分钟，中途崩溃前面工作全丢 | 断点续传：原子写入 + 快照，崩溃 5 秒内恢复 |
| 4 | 无风险拦截 | 可能推荐差评景点、超预算 45%、夜间高危路段 | HITL 人工审核：风险自动挂起给主管审批 |
| 5 | 假数据假智能 | 早期用写死种子数据 + Mock LLM，推理全是假的 | 真实数据源：高德 + 百炼，全链路真实 |
| 6 | 无人群适配 | 不管老人儿童，行程千篇一律 | 人群个性化：门票/交通按年龄分档、景点过滤、兴趣推荐 |
| 7 | 知识不沉淀 | 景区政策靠写死代码或 LLM 编造，会过时会失真 | RAG 检索增强：先查知识库，再基于真实资料生成 |

### 1.3 项目目标

**一句话目标**：把"人工排行程"改造成"AI 自动调研工厂"——输入一句自然语言，8 个 AI Agent 自动完成调研、编排、预算、报告，全程真实数据、可断点续传、可人工审核、可长期记忆、可人群适配、可检索增强。

### 1.4 工期与里程碑

- **1 人独立完成**；
- v1.0 工期 **7 人日**（任务一需求分析 1.4 / 任务二实现 3.5 / 任务三压测 + 任务四总结约 2.1）；
- 工单 7（图记忆 + 强干预）增量 **3 人日**；
- 后续持续迭代：人群适配、RAG、语音/对话式创建、高德地图总览等。

git 提交历史显示的关键里程碑（节选）：

| 阶段 | 关键提交 |
|---|---|
| v1.0 基建与 Agent 编排 | FastAPI/Postgres/Redis 基建、Streams Worker、LangGraph 8 节点、原子写 + checksum + 快照、注入检测 |
| HITL 闭环 | Sentiment + Itinerary + Budget + 条件边 HITL + Review API/CAS |
| 全栈与联调 | 前端看板/审核台、WebSocket、recover_all、kill -9 恢复脚本、两大场景联调 |
| 迭代增强 | 门票/车票按人群分档、一键启动脚本、RAG 升级（pgvector + 百炼 embedding）、人群个性化适配、语音/对话式创建、检索质量评估、高德地图总览 |

---

## 二、需求分析

### 2.1 核心功能需求（完整清单）

| # | 需求 | 说明 |
|---|---|---|
| 1 | 8 Agent 协同编排 | 偏好解析→任务拆解→调研→舆情→编排→预算→审核→报告 |
| 2 | 断点续传 | 全程持久化到 travel_plan.md，崩溃 5 秒恢复 |
| 3 | 状态机 + 原子写入 | planning/running/suspended/recovering/completed + failed/cancelled |
| 4 | HITL 人工审核 | 超预算 45% / 高危夜行 / 严重舆情 → 挂起人工审核 |
| 5 | 安全 | 内容过滤 + 防 Prompt 注入 + 审计日志 |
| 6 | 图记忆（工单 7） | 跨行程沉淀知识图谱，修复语义遗忘 |
| 7 | 运行态强干预（工单 7） | 主管带验签修改运行中状态 |
| 8 | 真实高德数据 | 景点/路线/天气/营业时间/评分/图片全真实 |
| 9 | 完整行程规划 | 每日三餐 + 景点图片 + 营业时间 + 日期自动算天数 |
| 10 | 往返交通规划 | 去程/返程方式 + 票价 + 耗时 + 途中建议 |
| 11 | 人群个性化适配 | 老人/儿童门票按年龄分档、交通购票分档、景点过滤 |
| 12 | RAG 检索增强 | 混合检索 + 引用溯源 + AI 目的地解读 |
| 13 | 三种创建方式 | 新建表单 / 语音创建 / 对话式创建 |
| 14 | 高德地图总览 | 景点位置标记 + 按天分组 + 路线连线 |
| 15 | 三角色 RBAC | 游客/顾问/主管权限分离 |
| 16 | 多国语言 | 11 种语言切换 |
| 17 | 手动编辑/删除行程 | 增删景点餐饮，不重跑 Agent |

### 2.2 非功能需求

| 指标 | 要求 |
|---|---|
| QPS | ≥ 200（status 查询为主） |
| P95 延迟 | < 300ms |
| 错误率 | < 0.1%（HTTP 5xx + 网关超时 + 50xxx/60xxx） |
| 记忆检索 P95 | < 150ms（工单 7 红线） |
| 干预成功率 | = 100%（工单 7 红线） |
| 崩溃恢复 | Worker 就绪后 5s 内 resume 未完成行程 |
| 部署 | Docker Compose 一键启动、健康检查自重启 |
| 监控 | Prometheus + Grafana 预置看板 |

### 2.3 验收场景（对照实现）

| 场景 | 内容 | 实现状态 |
|---|---|---|
| 场景 A | 正常行程生成（7 天云南家庭游 → 8 Agent → completed → 报告） | ✅ |
| 场景 B | kill -9 崩溃恢复（第 5 页后强杀，从第 6 页续跑，前 5 页不重复抓取） | ✅ |
| 场景 C | Prompt 注入拦截（第 7 页注入 → blocked + 审计 security_block，报告不含注入目标） | ✅ |
| 场景 D | HITL 挂起 + 审批（超预算 → suspended → 主管通过 → completed） | ✅ |

---

## 三、技术架构

### 3.1 六层架构（全景）

```
前端层    React 18 + TypeScript + AntD
         （10 个页面，11 国语言，WebSocket 实时推送）
            │ REST + WebSocket
API 网关  FastAPI + JWT/RBAC + 统一错误码 + HMAC 验签
            │ 入队 / 查询 / 干预
业务服务  高德服务 / 原子写 / 分布式锁 / 队列 / 恢复 / 安全过滤 / 注入检测
            │
Agent 编排 LangGraph StateGraph + 8 节点 DAG + 条件边（Worker 执行）
            │
数据层    PostgreSQL(pgvector，11 张表) + Redis + workspace 文件（travel_plan.md + 快照）
            │
外部数据源 高德地图（地理编码/POI/路径/天气）+ 阿里云百炼（LLM + Embedding + Rerank）
```

### 3.2 分层职责与关键约束

| 层 | 职责 | 关键约束 |
|---|---|---|
| 前端层 | 用户交互、状态展示、实时推送 | 不直连 Redis，只通过 API |
| API 网关层 | 鉴权、参数校验、路由、验签 | 不执行 Agent 长任务，只入队/查询 |
| 业务服务层 | 可复用业务逻辑 | 无进程内长期状态 |
| Agent 编排层 | 8 节点 DAG 执行、断点、记忆、干预 | 状态在 DB + 文件 + Redis，不在内存 |
| 数据层 | 持久化三件套 + 向量 | PG 权威、文件恢复、Redis 热状态 |
| 外部数据源 | 真实数据 | 带缓存 + 重试 + 降级 |

**三条分层禁令**（架构核心纪律）：

1. **API 进程不执行图**——API 只负责入队和查询，长任务全交给 Worker，保证 API 能扛住高 QPS；
2. **Agent 不持有进程内长期状态**——所有状态落 DB/文件/Redis，进程崩溃可恢复；
3. **前端不直连 Redis**——前端只通过 API，隔离数据层。

### 3.3 技术栈

| 层 | 技术 |
|---|---|
| 后端 | Python 3.12 / FastAPI / Pydantic v2 / SQLAlchemy 2 (async) / LangGraph |
| 队列/缓存 | Redis Streams（consumer group + PEL 断点续传）+ Lua |
| 数据库 | PostgreSQL 15 + pgvector（11 张表 + 审计日志） |
| 向量 | 阿里云百炼 text-embedding-v3（768 维） |
| LLM | 阿里云百炼 qwen3.7-flash（抽象 LLMProvider，Mock/真实可切换） |
| 前端 | React 18 / TypeScript / Vite / Ant Design（10 页面） |
| 部署 | Docker Compose（api / worker / redis / postgres / frontend / prometheus / grafana 7 服务） |
| 监控/压测 | Prometheus + Grafana + Locust / 自研 loadtest.py |

---

## 四、核心功能

### 4.1 8 Agent 协作编排（项目心脏）

8 个 Agent 按固定 DAG 拓扑协作，每个 Agent 分工明确、可独立降级：

```
START → ① Intake 偏好解析 → ② Planner 任务拆解 → ③ Web Research 调研
      → ④ Sentiment 舆情 → ⑤ Itinerary 日程编排 → ⑥ Budget 预算
      → 条件边（超预算45%/夜行/舆情 → ⑦ Human Review 审核；正常 → ⑧ Report）
      → ⑧ Report 报告生成 → END
```

各 Agent 职责：

| 顺序 | Agent | 输入 | 输出 |
|---|---|---|---|
| ① | Intake | 用户自然语言需求 | 结构化 preferences（目的地/天数/预算/出行人明细/兴趣标签） |
| ② | Planner | preferences | ≥10 条调研任务（关键词 + 高德 POI 类型，CoT 思维链） |
| ③ | Web Research | 任务列表 | 真实 POI 列表 + 知识库检索结果（ReAct 逐项调研，页级 checkpoint） |
| ④ | Sentiment | 全部抓取结果 | 风险标签（差评/强制购物/夜行等） |
| ⑤ | Itinerary | 偏好 + 调研 + 舆情 | 每日行程 + 往返交通 + 路线 + 门票分档（最复杂节点） |
| ⑥ | Budget | 行程 + 价格线索 | 分项预算（交通/住宿/门票/餐饮/其他）+ 超支比例 |
| ⑦ | Human Review | 风险上下文 | 无 LLM 输出，只做路由（挂起/放行） |
| ⑧ | Report | 以上全部 | 完整报告（概览/天气/交通/每日行程/餐饮/酒店/预算/数据来源 + RAG 解读） |

### 4.2 断点续传与崩溃恢复

- **原子写入**：mkstemp 临时文件 → write + flush → os.fsync 文件 → os.replace 原子替换 → fsync 目录，任何时刻崩溃都不会出现"写了一半"的文件；
- **checksum 校验**：读取时校验 YAML 头 SHA256，失败回退最近快照（保留 5 版）；
- **幂等恢复**：已 completed 且已落库的任务不重跑，只重跑无持久化结果的当前任务；
- **队列接管**：Redis Streams PEL + XAUTOCLAIM，Worker 崩溃后消息 2 秒自动被存活 Worker 认领；
- **实测**：kill -9 后重启，从 `resume_from=page_6` 续跑，已完成任务数不变（无重复抓取）。

### 4.3 HITL 人工审核

- **三个触发条件**（任一满足即挂起）：超预算 45% / 高危夜行（22:00–06:00）/ 严重舆情；
- 挂起后 Worker 释放（ack 消息），主管在审核台抢占（Lua CAS 防两人同时审批）→ 通过则重新入队续跑 / 驳回则终止；
- 审批超时 24h 自动驳回；DB `pending` 条件更新兜底防重复审批。

### 4.4 人群个性化适配

| 人群 | 适配逻辑 |
|---|---|
| 老人 | 门票 60–64 岁半价、65+ 免首道大门票；剔除滑雪/登山/雪山等高强度项目 |
| 儿童 | 门票 6 岁以下或 1.2 米以下免票、6–18 岁半价；高铁 6 岁以下免票不占座；剔除酒吧/夜店 |
| 学生 | 高铁学生票仅限家校往返（旅游不推荐）；飞机无学生优惠 |

- 交通购票按年龄分档（高铁/飞机），景点自动标注人群优待备注；
- 餐饮规范：早餐只吃早餐/快餐/轻食，三餐跨天不重复；
- 家庭模式夜生活只保留影院/剧场。

### 4.5 RAG 检索增强

- **知识库**：24 篇语料（景点攻略/地方美食/景区政策/四季玩法/交通出行/住宿/出行贴士/夜游拍照/城市问答），分块后经百炼 text-embedding-v3 向量化入库（181 chunk）；
- **混合检索**：pgvector 向量余弦（SQL 级）+ 语料级 IDF BM25 双路，RRF 融合，rerank 精排（网关未开放时降级 RRF 融合分）；多轮追问先经 LLM 改写为完整问题再检索；
- **引用溯源**：AI 目的地解读标注来源编号 [1][2]，报告末尾附参考来源对照表；
- **质量评估**：52 条测试集 Hit@1=84.6%、Hit@5 = 98.1%，MRR = 0.905。

### 4.6 图记忆与运行态强干预（工单 7）

- **图记忆**：节点产出文本 → LLM 抽取三元组（如 User-[HAS_ALLERGY]->Food:香菜）→ upsert 图节点/边 + 语义向量化 → 写流水；检索时 pgvector 余弦 + 图拓扑 1 跳邻域双路召回，Redis 缓存 60s（P95 0.3ms）；
- **强干预**：HMAC 验签 → nonce 防重放 → SETNX 排他锁 → 三写一事务（图节点 version+1 / interventions 流水 / Redis 补丁）→ 节点入口应用补丁（如剔除停运景点）。

### 4.7 安全体系

- JWT + RBAC（游客/顾问/主管三角色权限分离，顾问只查自己的行程、主管只审核不创建）；
- Prompt 注入检测（规则引擎 + `<web_content>` 结构隔离 + 工具白名单 fetch/search + 输出侧 sanitize）+ 内容安全过滤双模块；
- 干预接口 HMAC 验签 + nonce 防重放；审计日志全程留痕（登录/创建/审批/干预/删除/拦截）。

### 4.8 多种创建方式与地图总览

- **三种创建**：新建表单（精细控制）/ 语音创建（浏览器语音转文字）/ 对话式创建（AI 主动引导）；
- **地图总览**：行程详情集成高德地图，景点标记 + 按天分组 + 路线连线。

---

## 五、数据模型

### 5.1 数据源

| 数据源 | 能力 | 说明 |
|---|---|---|
| 高德地图 | 地理编码、POI 搜索、路径规划、天气、营业时间、图片 | 1 个 key 覆盖 6 类能力 |
| 阿里云百炼 | LLM（qwen3.7-flash）、Embedding（text-embedding-v3 768 维）、Rerank | 解析/抽取/票价估算/RAG 生成 |
| 文旅知识库 | 10 篇语料（61 chunk） | 本地 md 向量化入库 |
| 虚拟数据 | 房价/门票/人均估算 | 真实 POI 名 + 城市等级分档，标注参考价 |

> 密钥全部通过根目录 `.env` 注入（`.gitignore` 忽略，不入 git）。

### 5.2 数据库表（11 张）

**业务表（6 张）**：`users`（三角色）、`travel_plans`（状态/偏好/预算）、`agent_tasks`（8 类 Agent 执行记录）、`review_records`（人工审核）、`budget_records`（预算明细）、`audit_logs`（审计日志）

**图记忆表（4 张，工单 7）**：`graph_nodes`（含 pgvector 向量）、`graph_edges`（三元组关系）、`memory_events`（记忆抽取流水）、`interventions`（强干预流水）

**知识库表（1 张，RAG）**：`document_chunks`（知识库分块 + pgvector 向量）

---

## 六、API 与前端

### 6.1 API（6 组）

| 组 | 接口 |
|---|---|
| 认证 | register（含角色）/ login |
| 行程 | create / list / detail / status / agents / plan-file / report / cancel / delete / PATCH itinerary / chat |
| 审核 | pending / detail / claim / decision |
| 记忆 | intervene / rollback / graph / search / interventions |
| 运维 | health / metrics |
| 实时 | WebSocket `/ws/plans/{id}`（node_started / node_done / hitl_suspended / recovered / completed / heartbeat） |

### 6.2 前端（10 个页面）

登录/注册 ｜ 工作台 ｜ 行程列表 ｜ 新建行程（表单）｜ 语音创建 ｜ 对话式创建 ｜ 行程详情（交通+票价+地图总览+RAG 检索+每日行程+编辑+报告）｜ 审核台 ｜ 记忆图谱 ｜ 系统说明

---

## 七、测试与验证结果

### 7.1 性能压测（本机 Docker，20 并发 / 10 秒）

| 指标 | 实测 | 验收标准 | 结果 |
|---|---|---|---|
| QPS | **1109** | ≥ 200 | ✅ 超出 5.5 倍 |
| P95 延迟 | **35.4 ms** | < 300ms | ✅ 快 8 倍 |
| 错误率 | **0.000%** | < 0.1% | ✅ |

压测方式：`python deploy/loadtest.py 20 10`（纯 asyncio 并发）。

### 7.2 工单 7 红线

| 指标 | 实测 | 红线 | 结果 |
|---|---|---|---|
| 记忆检索 P95 | 0.3ms | < 150ms | ✅ 快 500 倍 |
| 干预成功率 | 100% | = 100% | ✅ |

### 7.3 RAG 检索质量评估（52 条测试集，覆盖 24 篇语料）

| 指标 | 数值 |
|---|---|
| Hit@1 | 84.6% |
| Hit@3 | 96.2% |
| Hit@5 | 98.1% |
| MRR | 0.905 |

### 7.4 端到端验证记录

- ✅ 10 页抓取（第 7 页注入 → blocked，第 8 页负面 → 舆情风险，第 9 页夜行 → 夜行风险）；
- ✅ 注入拦截审计 `security_block` 可查询，报告不含注入目标；
- ✅ HITL 挂起 → 审批通过 → completed 完整闭环；
- ✅ kill -9 后重启，从 `resume_from=page_6` 续跑，无重复抓取；
- ✅ 容器化完整 E2E：登录 → 创建行程 → 8 Agent 执行 → HITL 挂起 → 审批通过 → completed → 报告含每日行程。

### 7.5 测试脚本清单

| 脚本 | 用途 |
|---|---|
| `python -m pytest tests/test_memory_intervention.py -v` | 单元测试（图记忆 + 干预） |
| `python scripts/e2e_check.py` | 端到端联调（场景 A/C/D） |
| `python scripts/recovery_test.py` | 崩溃恢复（场景 B） |
| `python scripts/simulate_conversation.py` | 15 轮对话模拟 |
| `python scripts/concurrent_intervention_test.py` | 50 并发竞态审计 |
| `python scripts/verify_retrieve_latency.py` | 检索延迟验证 |
| `python scripts/eval_retrieval.py` | RAG 检索质量评估 |
| `python deploy/loadtest.py 20 10` | 压测 |

---

## 八、项目亮点与设计决策

### 8.1 项目亮点（10 项）

1. **全链路真实数据**：高德 + 百炼，无假数据假智能；
2. **断点续传 + 原子写入**：崩溃 5 秒恢复，不丢进度不重复执行；
3. **人群个性化适配**：老人/儿童/学生按年龄/身高分档，行程千人千面；
4. **RAG 检索增强完整闭环**：混合检索 + rerank + 引用溯源 + 知识库 + 系统化评估 + 前端展示；
5. **HITL 人工审核**：风险自动挂起，主管门控；
6. **图记忆 + 强干预**：跨行程记住偏好，运行态实时纠偏；
7. **三种创建方式 + 地图总览**：表单/语音/对话式 + 高德地图总览；
8. **性能大幅超标**：QPS 5.5x、P95 快 8x、检索快 500x、干预成功率 100%；
9. **工程细节扎实**：原子写 + checksum + 快照、Lua CAS + DB 兜底、防抖 + 幂等 + 超时 + 心跳、密钥安全注入；
10. **容器化暴露并修复真实 bug**：写入-入队竞态（入队前显式 commit）、nginx 502、百炼 embedding batch 上限（10 不是 16）。

### 8.2 关键设计决策

| 决策 | 内容 | 理由 |
|---|---|---|
| 编排框架 | LangGraph | 工单首选，7 人日不允许自研图引擎 |
| 队列 | Redis Streams | PEL 原生断点，比 Celery 轻 |
| LLM | 抽象 Provider，Mock→真实切换 | 压测不烧钱，演示切真模型 |
| 文件布局 | 每行程一目录 | 避免并发写冲突 |
| 恢复粒度 | task/网页级 | 满足验收，ReAct step 级是加分项 |
| 图存储 | PostgreSQL + pgvector | 单机原则，不引 Neo4j |
| 记忆引擎 | 自研轻量 Mem0 风格 | 3 人日不允许消化外部框架黑盒 |
| 干预 | HMAC + nonce + 三写事务 | 工单红线：安全验签 |
| 门票分档 | 纯函数 + 可配阈值 | 规则可调，不写死 |
| 知识库 | 本地 md + 向量化入库 | 改 markdown 即可更新 |

### 8.3 已知限制与后续规划

- **rerank 降级**：当前 MaaS 网关未开放 rerank 接口，检索重排走关键词打分降级；代码已实现真 rerank，网关开放后自动启用；
- **单 Worker**：MVP 单 Worker 部署，Consumer Group 已预留多 Worker 扩展（按 plan_id 亲和或行程级锁）；
- **二期明确不做**：自定义 DAG 引擎、MinIO、refresh token、roles 权限矩阵、React Flow、审核 rework 回 Planning、Web Research∥Sentiment 并行、PDF 导出、ReAct step 级恢复；
- **可扩展方向**：多 Worker 生产扩容、PDF 报告导出、rework 二次规划、更多真实数据源接入。

---

*资料来源：项目讲解 v4.0（2026-09-05）、需求与技术方案说明书 v1.0（2026-08-21）、产品介绍 v3.1（2026-09-05）、交付文档 v2.0（2026-08-28）、README.md、git 提交历史。*
